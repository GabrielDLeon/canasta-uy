# Production-specific overrides
server:
  error:
    include-message: never
    include-stacktrace: never

canasta:
  security:
    jwt:
      secret: "${JWT_SECRET}"

management:
  endpoint:
    health:
      show-details: never
